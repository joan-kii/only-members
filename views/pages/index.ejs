<%- include('../partials/header'); %>
<main class="relative flex-1">
  <div class="container mx-auto mt-20 mb-5 p-6 w-2/4 bg-gray-100 shadow-2xl rounded ring ring-inset ring-yellow-200">
    <% if (currentUser) { %>
      <p><%= currentUser %></p>
    <% } else { %>
      <p>LOL</p>
    <% } %>
  </div>
</main>
<%- include('../partials/footer'); %>
